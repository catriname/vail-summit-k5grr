# IDE and editor directories
.claude/
.vscode/
.idea/

# Project-specific files (not for distribution)
CLAUDE.md

# Build artifacts
build/
*.elf
*.hex
*.map

# Ignore .bin files except in firmware_files/
*.bin
!firmware_files/*.bin

# Standalone Arduino CLI environment (large, regeneratable)
arduino-cli/

# Arduino CLI cache/data (created during builds)
data/

# Local secrets (for development with real API keys)
secrets_local.h
src/core/secrets_local.h
.env
*.env.local

# OS files
.DS_Store
Thumbs.db
